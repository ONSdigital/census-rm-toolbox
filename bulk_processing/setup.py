import setuptools

setuptools.setup(
    name="bulk_processing",
    version="4.1.0a",
    description="Process bulk events from files into RM event messages",
    url="https://github.com/ONSdigital/census-rm-toolbox/tree/master/bulk_processing",
    packages=setuptools.find_packages(),
)
